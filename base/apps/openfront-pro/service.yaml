# openfront-pro Service
# Wave 6
---
apiVersion: v1
kind: Service
metadata:
  name: openfront-pro
  namespace: default
spec:
  selector:
    app: openfront-pro
  ports:
    - port: 80
      targetPort: 3000
