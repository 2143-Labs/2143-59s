
# mongo-imagehost Service — image host MongoDB
# Wave 6
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-imagehost
  namespace: default
spec:
  selector:
    app: mongo-imagehost
  ports:
    - port: 27017
      targetPort: 27017