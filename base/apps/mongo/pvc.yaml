
# mongo-imagehost PVC — separate storage from main MongoDB
# Wave 6
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-imagehost-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn