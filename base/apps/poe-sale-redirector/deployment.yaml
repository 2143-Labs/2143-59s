# poe-sale-redirector — stateless redirect service (raw IP, no ingress)
# Wave 6
# Runs on raw IP. No TLS via cert-manager (game traffic).
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: poe-sale-redirector
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: poe-sale-redirector
  template:
    metadata:
      labels:
        app: poe-sale-redirector
    spec:
      hostNetwork: true
      containers:
        - name: app
          image: ghcr.io/john2143/poe-sale-redirector:latest
          ports:
            - containerPort: 8080
              hostPort: 8080
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 128Mi
              cpu: 200m
---
apiVersion: v1
kind: Service
metadata:
  name: poe-sale-redirector
  namespace: default
spec:
  selector:
    app: poe-sale-redirector
  ports:
    - port: 8080
      targetPort: 8080
